spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/kino

jimmer:
  #  dialect: postgre
  show-sql: true
  language: kotlin
server:
  port: 8180
